# 游戏内存研究工具 — 功能与使用说明（自用）

> 稳定版本 = 当前 `overlay/build/overlay.exe` + `overlay/build/xixing7.dll`。
> 本文档仅记录功能与操作，供自己使用。

## 一、组成
- **overlay.exe**：覆盖窗（ESP/HUD）+ 全局热键 + M4 自动化流程。启动时把 DLL 注入游戏；**游戏关闭时自动退出**（方便手动重新拉起）。
- **xixing7.dll**：注入到游戏进程，安装 `ProcessEvent` 钩子 + 命令队列（RPC 都在游戏线程上执行）。

## 二、启动
1. 进入对局（真实对局，主城/大厅无效）。
2. 运行 `overlay.exe` —— 自动注入 DLL、出 ESP。
3. 每次重启游戏后，需要**重新拉起一次 overlay**。

## 三、功能与热键
| 键 | 功能 | 说明 |
|----|------|------|
| ESP | 容器/掉落物透视 + 中文 HUD | 箱子按品质(白/紫/金/红)着色；只高亮金/红掉落 |
| **M4** | 自动开箱 + 采矿/灵丛 | 见下方“M4 用法” |
| **F9** | 增伤 +200000 | 打怪用（也用于满足反作弊的“打怪”要求） |
| **F10** | 回血 | ServeraddHP |
| F3 | 魂珠 | 仅对局内有效，作用有限 |
| F4 / F5 | 经济项 | 效果不明显 |

### M4 用法（重要）
1. 先用 **F 手动开一个金/红箱**，等约 2 秒 —— 目的是 latch 到“交互技能 handle”（M4 依赖它）。
2. 靠近箱子 / 矿 / 灵丛，按 **M4**。
3. 第一次按是**安全轮**（验证 + 开 1 个），再按一次才跑满。
4. 每轮上限 6 个；顺序：红 > 金 > … 箱子优先，**矿/灵丛排在箱子之后**。
5. 开完自动拾取范围内金/红掉落物。

## 四、使用节奏 / 注意
- **反作弊**：短时间（约 1~2 分钟）内连续交互约 8 次即被踢出，实测打怪不重置计数。节奏 = 开 5–6 个 → 停顿片刻 → 再开 5–6 个。
- M4 必须先有 latch（先 F 开一个箱）。
- 只在**真实对局**内有效。

## 五、已知做不到（主机权威，别指望）
- **全图传送**：`ServerTeleport` 客户端能调用，但主机不认这些标签（所有测试 CALLED 但人不动）。
- **改移动速度 / 跳跃 / 飞行**：本地写会被主机回弹。
- **直接加物品**：`ServerAddItem` 等无效（背包不变）。
- **无敌**：做不到；只能增防 + 回血。

## 六、当前保留点（矿/灵丛）
- 矿(`BP_Mining_C`)/灵丛(`BP_Collect_C`)已纳入 M4：用每节点自己的交互组件(`+0x2C8`)，和开箱同一套 `SetPre + TryActivate` 驱动。
- 但这两类**“采集完成”的判定信号尚未最终确认**（箱子是 `+0x2B8==3`，工具节点未知）。当前实现是**每个节点尝试一次（尽力而为）**，用“已尝试”集合避免空转。
- 待实测：确认采集完成信号后再精修（是否需要重复驱动、完成后从列表剔除等）。
